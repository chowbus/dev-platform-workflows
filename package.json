{
  "name": "web-deploy-test",
  "version": "1.0.0",
  "description": "Simple test project for web deployment pipeline",
  "scripts": {
    "build": "echo 'Building...' && mkdir -p build && cp index.html build/",
    "build_staging-01": "npm run build",
    "build_staging-02": "npm run build",
    "build_staging-03": "npm run build",
    "build_production": "npm run build"
  }
}
